# ===================== Spring =====================
server.port: 8181

# =================== Datasource ===================
# Postgres
spring:
  jpa:
    hibernate.ddl-auto: none
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: jdbc:postgresql://localhost:5432/job_searching
    username: user
    password: password